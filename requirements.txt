# Dependencias de Accesos Directos.
# Instalar con: pip install -r requirements.txt
# (iniciar.bat ya lo hace automáticamente si detecta que faltan)

pywin32>=306; sys_platform == "win32"   # Iconos reales de Windows para cada acceso.
Pillow>=10.0.0                          # Conversión de iconos a imágenes que Tkinter puede mostrar.
tkinterdnd2>=0.3.0                      # Arrastrar archivos desde el Explorador de Windows.
